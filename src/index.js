import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

//Render html in component App to root
//Components must return what can be displayed on browser (HTML code)
ReactDOM.render(<App/>,document.getElementById('root'));